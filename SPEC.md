# 基于 Python 的在线考勤系统

本机统一开发，一个后端、一个 SQLite 数据库。教师创建指定班级任务，学生获取当次浏览器定位，后端校验后保存成功签到，教师查看全班结果，学生查看个人记录。

## 原始四表与管理扩展表
- student：id、唯一 student_no、name、class_name、password_hash。
- teacher：id、唯一 teacher_no、name、password_hash。
- attendance_task：id、title、teacher_id、target_class_name、start_time、end_time、center_latitude、center_longitude、radius_meters、created_at。
- attendance_record：id、task_id、student_id、checkin_time、latitude、longitude、distance_meters、status（仅 success）。task_id + student_id 联合唯一。

SQLite 启用外键；账号密码使用 Werkzeug 哈希；所有 POST 都校验 CSRF。session 同时存角色和用户 id，服务器校验角色、任务所有权和班级。学生身份、签到时间、距离不接受客户端指定。

## 签到规则
每项任务发布时按 class_name 固定目标学生名单。转班仅影响此后新建的任务，已有任务名单保持不变；不新增班级实体表。教师共享学生名册并维护姓名/班级，只调整自己发布任务的签到；学生只访问固定名单中包含自己的任务及本人记录。

北京时间 UTC+08:00 输入/显示，数据库 datetime 全部为无时区 UTC。统一服务端时钟，start_time <= now <= end_time；未开始、进行中、已结束统一计算。

Haversine 公式使用地球半径 6371000 米。经纬度合法且有限，半径有限且大于零，未经四舍五入的距离 <= 半径才成功。失败不存记录，允许重试；唯一约束处理重复并 rollback，其他数据库错误不误报重复。

教师结果使用发布时固定名单。有效签到由原始定位记录与最新人工调整共同决定；补签计入已签到并标注来源，撤销从到场人数扣除，原始记录保留。无有效签到按任务时间显示未开始/未签到/缺勤。

## 非目标与边界
不包含人脸、二维码、地图选点、请假、排课、管理员、注册、AI、微服务、Docker 或复杂图表。不保证防模拟定位、代签到或定位误差。默认仅本机 HTTP、debug 关闭；手机可信 HTTPS 环境另行配置，未实测不得宣称通过。

## 地图展示升级（2026-09-21）
使用本地 Leaflet 1.9.4 与在线 OpenStreetMap 标准底图。教师创建时预览中心与范围，半径/中心变化实时更新；教师结果可选择一条成功记录展示其位置；学生展开本班任务范围，签到定位后显示本次位置与精度。

地图折叠时不请求瓦片，展开按需加载；按最新需求移除本地 SVG 范围示意图。底图失败或超时显示简洁提示，可点击“重新加载地图”重试。所有界面距离仅作参考，最终仍以服务器校验为准。定位仅在原有按钮点击时获取，不新增追踪；不同学生间不暴露位置。地图失败不阻断签到，保留原始定位记录、UTC 和后端围栏判断规则。

无 API Key、无新 Python 依赖或 Node 构建。保留 OSM 署名，允许跨站请求发送 origin Referer，使用浏览器正常 HTTP 缓存，不提供地图瓦片预取/离线下载。加载在线地图会向 OSM 请求所显示区域的瓦片，不传学生姓名、账号或考勤表。

## 界面升级

教师和学生任务列表提供客户端状态筛选、关键词搜索、匹配计数与清除操作，筛选范围仅为后端授权返回的任务。导航选中态、返回/刷新/取消和主要操作采用统一按钮样式；签到成功实时更新卡片筛选状态。窄屏使用单列布局，保留可访问的键盘焦点和状态反馈。

## 学生管理扩展

新增四表：task_roster（固定标记，包含空名单）；task_member（task_id + student_id 主键、原班级）；student_change（姓名/班级前后值、教师、原因、UTC 时间、每生版本）；attendance_adjustment（任务、学生、教师、present/absent/restore、原因、UTC 时间、每任务学生版本）。后两者保留历史，版本联合唯一；补签不向 attendance_record 写入伪位置。

教师目录可搜索和分页，修改姓名和所属班级，原因必填。已发布任务不随学生班级变动而变更名单。签到状态页面同时保留原始证据和修改历史。教师补签、撤销和恢复受任务所有权、固定名单、任务已开始、CSRF 和版本校验约束；学生仅能看到本人调整记录。学生定位签到不能覆盖人工补签/撤销决定。

升级脚本先执行 SQLite 一致性备份，再新增扩展表、补齐旧名单；旧四表逐行保留。缺少历史名单的旧任务只能依据升级时班级及已存定位记录重建。
