# 实际验收记录

执行日期：2026-09-21。本机 Windows / PowerShell 7.6.5 / Python 3.13.3，项目虚拟环境 `.venv`。首次开发时为空目录，以下早期验收为历史记录；最新 GitHub 交付回归见文末。

## 自动化测试

实际执行：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

真实输出：

```text
........................................................................ [ 88%]
.........                                                                [100%]
81 passed in 45.68s
```

| 验证内容 | 实现与覆盖 |
|---|---|
| 登录/退出、错误密码、清除旧状态、同 id 不同角色 | test_auth.py |
| 未登录、角色越权、教师任务归属、全部 POST 的 CSRF | test_auth.py |
| 正常创建、非法标题/班级/时间/经纬度/半径、自动转义 | test_teacher.py |
| 班级名单与人数、未开始/未签到/缺勤、其他班级排除 | test_teacher.py |
| 学生班级范围、客户端伪造身份/时间/距离无效、个人记录隔离 | test_checkin.py |
| 围栏外失败后重试、重复提交、NaN/Infinity/空值等非法参数 | test_checkin.py |
| 起止时间闭区间、距离等于半径、微小越界不因取整通过 | test_checkin.py |
| 真正联合唯一约束与回滚、第二连接竞争写入、其他数据库错误不误报重复 | test_checkin.py |
| Haversine 同点/内外/对跖点/跨经度边界、UTC 与北京时间转换 | test_services_and_scripts.py |
| SQLite 外键、重复初始化/种子数据、密码时间不覆盖、不兼容旧表保留 | test_services_and_scripts.py |
| 开发随机密钥、HTTP/HTTPS Cookie 配置 | test_services_and_scripts.py |

每个业务测试使用 pytest 临时目录的独立 SQLite 数据库。CSRF 没有关闭，使用页面取得的 token 测试合法请求，缺失/无效/别的 session 的 token 被拒绝。时间边界通过固定时钟测试，无 sleep。

## 初始化、依赖与启动

已实际执行：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m scripts.init_db
.\.venv\Scripts\python.exe -m scripts.init_db
.\.venv\Scripts\python.exe -m scripts.seed_data
.\.venv\Scripts\python.exe -m scripts.seed_data
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe app.py
```

- 初始化重复成功，保留数据。
- 首次种子任务新建 6 个，再次新建 0 个；2 教师、10 学生、两个班级，已结束任务 4 条合成记录。
- 再次运行脚本明确提示旧演示任务已结束，不会修改旧任务时间。
- pip check：`No broken requirements found.`
- 启动输出：`Debug mode: off`，`Running on http://127.0.0.1:5000`。
- HTTP 页面通过实际浏览器访问，CSS 与 JavaScript 均本地加载。
- 已生成 requirements-lock.txt，记录实际安装版本；无需升级全局 Python。
- 另实际执行 `python -m pip install -r requirements-lock.txt`，所有锁定版本均已满足。
- 浏览器验收后直接核查开发数据库：teacher=2、student=10、attendance_task=7、attendance_record=5；`PRAGMA foreign_keys` 为 1，`PRAGMA integrity_check` 为 `ok`，重复 task_id + student_id 分组为空。额外的 1 个任务和 1 条记录来自明确标记的模拟验收。

## 实际浏览器验收

使用 Codex 内置浏览器访问本机服务，已检查登录页、教师任务列表、创建页、结果页、学生班级列表、个人记录，并截图检查窄窗口排版。

实际完成的数据库联动：

1. T001 教师登录并通过表单创建 `[模拟验收] 浏览器签到闭环`，软件一班、200 米、中心 `(39.9042, 116.4074)`。
2. 切换 S001 学生，能看到本班任务；未开始任务不可点，预置已签到任务显示已签到。
3. 初次使用原生定位请求出现超时，页面显示中文超时提示，按钮恢复为“重新定位并签到”。不能据此宣称真实位置获取成功。
4. 为检验前后端闭环，仅在测试标签页临时替换浏览器 geolocation API 返回合成位置（未改项目 JS 或正式接口，未绕过后端）。纬度 `39.9042`、经度 `116.4174`、精度 `10` 米时，显示“距离签到中心约 853.00 米，允许半径 200 米，超出约 653.00 米。”。
5. 改合成坐标为中心点重试，显示“签到成功，距离签到中心约 0.00 米”，按钮禁用为已签到。
6. 在预先打开的旧学生页面再次提交，返回“你已签到，请勿重复提交”。
7. 个人记录出现该次成功签到；教师结果为应到 5、已签到 1、未签到 4，并显示 S001 的服务器时间和 0.00 米距离。
8. 教师定位按钮也用合成坐标检查，正确填入中心；精度 `300` 米时显示精度差的提醒，半径仍为 `200` 米。
9. 浏览器错误/警告日志检查：主验收标签页返回空列表；重复接口的业务状态为 409，属于预期拒绝。

临时浏览器定位覆盖已清理，页面导航恢复原生 API，额外旧页面已关闭。数据库保留 1 个明确命名的模拟验收任务和 1 条成功记录，方便复查，不代表真实到场。

## 未实测与边界

- 真实物理位置获取与真实围栏内成功：未实测成功；当前内置浏览器原生位置请求超时。
- 真实手机、局域网 HTTPS、不同设备定位精度：未实测，未部署网络服务。
- 首版不含地图（后续地图升级验收另行记录）；反模拟定位、防代签到、历史转班等仍为非目标。
- 浏览器开发者工具模拟与 pytest 坐标测试只能证明流程/公式/规则，不能证明设备真实位置。

现场人工验收步骤在 DEMO.md；核心需求已实现，无核心 TODO。

## 首次地图升级回归（历史记录，示意图已移除）

接入 Leaflet + OpenStreetMap 和本地 SVG 示意图后，新增 `tests/test_maps.py` 的 5 项测试，覆盖班级范围、教师任务与记录隔离、本地资源、非法表单和客户端地图参数不能绕过后端。

最终实际执行 `.\.venv\Scripts\python.exe -m pytest -q`，结果：

```text
........................................................................ [ 83%]
..............                                                           [100%]
86 passed in 48.29s
```

已重启本机服务并实际浏览器验证：在线底图加载、范围与半径联动、记录位置、本地示意图、底图请求被阻断时自动降级、网络恢复后重试，以及地图组件失效时仍能完成正式签到。检查未发现未捕获 JavaScript 异常；故障模拟产生的瓦片网络错误和圈外签到的 HTTP 400 属于预期。

本次另保留 `[模拟验收] 地图升级` 任务及 1 条合成成功记录，未修改用户已有任务。详细过程和限制见 [地图升级验收](MAPS.md)。

## 移除示意图与界面升级验收（2026-09-21）

- 已删除本地示意图绘制、切换按钮和专用样式；在线地图失败时只显示提示与“重新加载地图”。本地保留的 SVG 仅为普通界面图标，Leaflet 的范围覆盖物仍用于真实在线地图。
- 教师、学生任务列表加入状态筛选、关键词搜索、结果计数、清除筛选；已签到状态即时更新。隐藏任务时折叠地图，停止对应瓦片加载。
- 统一主次按钮、图标、导航选中态、卡片和表格；增加结果/记录页返回与刷新入口，已签到卡片提供个人记录入口。支持键盘焦点、跳至主要内容和减少动画偏好。
- 浏览器实测：教师状态筛选、关键词搜索、空结果、清除筛选、结果页往返、创建页取消；学生状态筛选、个人记录跳转、刷新与返回。
- 验证 1200 × 900 桌面和 390 × 844 窄屏布局；所检查页面没有文档级横向溢出，长表格保留内部横向滚动。
- 仅阻断 OSM 瓦片请求后，页面显示失败提示；恢复请求并点击重试后真实地图恢复加载，无本地示意图。
- 在已有 `[模拟验收] 地图升级`（id=9）任务中，以演示学生 S005 和合成坐标 `(39.9042, 116.4074)`、精度 10 米验证签到。底图被阻断时，服务器仍返回签到成功，距离 0.00 米。卡片立即转为已签到；切换“可以签到”不再显示该任务，“已签到”可查到该任务；个人记录可查到本次记录。
- 本次保留 1 条 S005 合成签到记录（2026-09-21 17:59:06 北京时间）；该模拟任务现有 S001、S005 共 2 条记录。未修改用户其他任务与记录，没有清空数据库。
- 浏览器未发现未捕获 JavaScript 异常。网络阻断、合成定位和临时视口尺寸已恢复，页面停留教师工作台。
- 全套回归：`.\.venv\Scripts\python.exe -m pytest -q`，**86 passed in 49.04s**。同步更新使用说明与地图测试断言。

手机布局使用浏览器窄屏模拟；真实手机硬件定位精度不属于本次实测结论。

## 学生信息管理与签到调整回归（2026-09-21）

新增 26 项管理功能测试，全套 **112 passed in 74.51s**。覆盖权限隔离、班级固定名单、补签/撤销/恢复、来源与位置区分、前后端状态一致、旧表单冲突、原因校验、事务回滚及旧库升级。

浏览器完成教师管理入口、查询、转班、学生端同步、补签和撤销恢复的闭环。验收使用已有模拟任务及演示账号，结束后恢复原所属班级与有效签到状态；原始四表与升级前备份逐行一致，仅保留明确标注的调整审计。完整步骤、备份路径与数据核对见 [MANAGEMENT.md](MANAGEMENT.md)。

## GitHub 交付前完整回归（2026-09-21）

- 重新执行全套测试：**112 passed in 73.51s**，覆盖登录、权限、CSRF、围栏与时间边界、重复签到、统计、转班固定名单、补签/撤销/恢复、数据升级和异常回滚。
- `python -m pip check`：No broken requirements found；应用、路由、服务和脚本的 Python 编译检查通过。
- 再次通过浏览器检查教师学生查询、信息页、签到调整页、统计结果、在线 OSM 地图加载、创建页取消和状态筛选，均正常。
- 发布前检查上传清单：排除 SQLite 数据库、备份、虚拟环境、缓存、真实环境配置和密钥文件；仅保留 `instance/.gitkeep` 与 `.env.example`。凭据格式扫描未发现匹配项，示例账号密码是明确标记的课程演示数据。
- 新增 GitHub Actions：固定第三方 Action 提交，在 Ubuntu / Python 3.13 的干净环境安装锁定依赖并运行相同测试。各次远程执行结果以仓库 Actions 页面为准。
